apiVersion: apps/v1
kind: Deployment
metadata:
  name: qemu1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: qemu1
  template:
    metadata:
      labels:
        app: qemu1
    spec:
      containers:
        - name: qemu
          image: qemux/qemu
          env:
            - name: BOOT
              value: "ubuntu"
            - name: QEMU_ARGS
              value: "-vnc 0.0.0.0:0"
          volumeMounts:
            - name: storage
              mountPath: /storage
          securityContext:
            capabilities:
              add: ["NET_ADMIN"]
        - name: websockify
          image: lukaskellerstein/websockify:0.0.1
          args: ["80", "localhost:5900"]
          ports:
            - containerPort: 80
      volumes:
        - name: storage
          persistentVolumeClaim:
            claimName: qemu1-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: qemu1
spec:
  selector:
    app: qemu1
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qemu1-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: qemu1-ingressroute
spec:
  entryPoints:
    - web
  routes:
    - match: PathPrefix(`/qemu1`)
      kind: Rule
      services:
        - name: qemu1
          port: 80